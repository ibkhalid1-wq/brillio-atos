# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
dist-ssr/
*.local

# Local source backups (git bundles + tarballs)
backups/

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Shareable env template (public VITE_ values; keep out of git anyway)
.env.local.shared

# The Laila film. Its source is worth keeping, but this working copy carries
# ~50MB of extracted screen-recording clips plus symlinks to the Aura
# project's node_modules and music bed — none of which belong in git.
video-laila/
video/public/laila/

.proof.cjs

# Audit/session scratch — working notes from an investigation, not source.
audit-scratch/
